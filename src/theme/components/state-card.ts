import { radius } from '../foundations/radius';

export const feedStateCardTokens = {
  minHeight: 292,
  radius: radius.sm,
  paddingHorizontal: 16,
  paddingVertical: 12,
  gap: 16,
  headerHeight: 40,
  headerGap: 12,
  avatarSize: 40,
  illustrationSize: 112,
} as const;
