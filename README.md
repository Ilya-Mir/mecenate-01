# Mecenate Feed Test

## Запуск

```bash
npm install
npm start
```

Дальше откройте приложение в Expo Go по QR-коду из терминала.

Симуляторы:

```bash
npm run ios
npm run android
```

При необходимости скопируйте `.env.example` в `.env` и задайте переменные с префиксом `EXPO_PUBLIC_`.
